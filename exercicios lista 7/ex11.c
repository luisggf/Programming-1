#include <stdio.h>

int main()
{
    int base, i, j;
    printf("Valor da base: ");
    scanf("%d", &base);
    for (int i = 1; i <= base; i++)
    {
        for (int j = 1; j <= base; j++)
            if ()
                printf(" * ");
        printf("\n");
    }

return 0;
}


        